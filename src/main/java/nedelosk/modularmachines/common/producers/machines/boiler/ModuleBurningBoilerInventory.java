package nedelosk.modularmachines.common.producers.machines.boiler;

public class ModuleBurningBoilerInventory /*
											 * extends
											 * ModuleMachineRecipeInventory<
											 * ModuleBurningBoiler>
											 */ {
	/*
	 * public ModuleBurningBoilerInventory(String categoryUID, String moduleUID,
	 * int slots) { super(categoryUID, moduleUID, slots); }
	 * @Override public List<Slot> addSlots(IContainerBase container,
	 * IModularInventory modular, ModuleStack<ModuleBurningBoiler> stack) {
	 * ArrayList<Slot> slots = new ArrayList<Slot>(); slots.add(new
	 * SlotModular(modular.getMachine(), 0, 80, 34, stack)); return slots; }
	 */
}
